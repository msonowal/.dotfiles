# ============================================================================
# Oh My Zsh
# ============================================================================

export ZSH="$HOME/.oh-my-zsh"
ZSH_THEME=""  # Using Starship prompt instead

plugins=(
  git
  docker
  docker-compose
  fzf
  zoxide
  zsh-autosuggestions
  zsh-syntax-highlighting
)

source $ZSH/oh-my-zsh.sh

# ============================================================================
# PATH
# ============================================================================

export PATH="$HOME/.local/bin:$PATH"
export PATH="$PATH:$HOME/.composer/vendor/bin"

# Bun
export BUN_INSTALL="$HOME/.bun"
export PATH="$BUN_INSTALL/bin:$PATH"

# ============================================================================
# Environment
# ============================================================================

export LANG=en_US.UTF-8
export LC_ALL=en_US.UTF-8
export EDITOR="vim"

# ============================================================================
# fnm (Fast Node Manager)
# ============================================================================

FNM_PATH="/opt/homebrew/opt/fnm/bin"
if [ -d "$FNM_PATH" ]; then
  eval "$(fnm env)"
fi

# ============================================================================
# fzf
# ============================================================================

[ -f ~/.fzf.zsh ] && source ~/.fzf.zsh

# Use fd for file search, bat for preview, Tokyo Night colors
export FZF_DEFAULT_COMMAND='fd --type f --hidden --follow --exclude .git'
export FZF_CTRL_T_COMMAND="$FZF_DEFAULT_COMMAND"
export FZF_ALT_C_COMMAND='fd --type d --hidden --follow --exclude .git'
export FZF_CTRL_T_OPTS="--preview 'bat --color=always --style=numbers --line-range=:500 {}'"
export FZF_DEFAULT_OPTS="
  --height 40% --layout=reverse --border
  --color=bg+:#3a2a4d,bg:#262335,spinner:#ff7edb,hl:#36f9f6
  --color=fg:#e0def4,header:#36f9f6,info:#ff7edb,pointer:#fe4450
  --color=marker:#72f1b8,fg+:#f0f0f0,prompt:#ff7edb,hl+:#36f9f6"

# ============================================================================
# Bun completions
# ============================================================================

[ -s "$HOME/.bun/_bun" ] && source "$HOME/.bun/_bun"

# ============================================================================
# direnv (per-directory env vars)
# ============================================================================

eval "$(direnv hook zsh)"

# ============================================================================
# mise (version manager)
# ============================================================================

# eval "$(mise activate zsh)"  # disabled for shell speed — re-enable when needed

# ============================================================================
# Aliases
# ============================================================================

# Modern CLI replacements
alias cat='bat --paging=never'
alias ls='eza --icons --group-directories-first'
alias ll='eza --icons --group-directories-first -la'
alias lt='eza --icons --tree --level=2'
alias top='htop'
alias diff='delta'

# Git
alias lg='lazygit'

# Docker
alias docker_p="docker ps --format 'table {{.Names}}\t{{.Status}}\t{{.RunningFor}}\t{{.Image}}\t{{.ID}}'"
alias lzd='lazydocker'

# Laravel
alias art='php artisan'
alias artm='php artisan migrate'
alias artmf='php artisan migrate:fresh --seed'
alias tinker='php artisan tinker'
alias sail='[ -f sail ] && sh sail || sh vendor/bin/sail'

# ============================================================================
# Starship prompt (must be at end)
# ============================================================================

eval "$(starship init zsh)"

# ============================================================================
# Reference: Previously used settings (kept for reference)
# ============================================================================
# Flutter:  export PATH="$PATH:/Users/msonowal/development/flutter/bin"
# Android:  export ANDROID_HOME=/Users/msonowal/development/android-sdk
# Java:     export JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home"
# Node 18:  export PATH="/opt/homebrew/opt/node@18/bin:$PATH"
# Claude:   alias claude="/Users/msonowal/.claude/local/claude"

# Go
export PATH="$HOME/go/bin:$PATH"
